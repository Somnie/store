<% if controller.action_name == "show" %>
<%= link_to("Home", services_path) +" > " + link_to("#{@service.name}", service_products_path(@service)) + " > " + "#{@product.name}" %>
<% else %>
<%= link_to("Home", services_path) +" > " + "#{@service.name}" %>
<% end %>